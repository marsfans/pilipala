name: Build & Release Transmission

on:
  workflow_dispatch:
    inputs:
      version:
        description: "Release version (e.g. 4.0.6)"
        required: true
        type: string
        default: '4.0.6'

jobs:
  build:
    strategy:
      matrix:
        os: [ubuntu-24.04, macos-latest]

    runs-on: ${{ matrix.os }}
    
    steps:
      - name: Checkout source
        uses: actions/checkout@v4
        with:
          repository: transmission/transmission
          ref: ${{ github.event.inputs.version }}
          submodules: recursive
          fetch-depth: 0

      - name: Determine version
        id: version
        shell: bash
        run: echo "version=${{ github.event.inputs.version }}" >> $GITHUB_OUTPUT

      # -------- Dependencies --------
      - name: Install deps (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt update
          sudo apt install -y \
            build-essential cmake ninja-build \
            libcurl4-openssl-dev libssl-dev libevent-dev libdeflate-dev \
            libgtk-4-dev libgtkmm-4.0-dev libglibmm-2.68-dev libgiomm-2.68-dev \
            qtbase5-dev qttools5-dev qttools5-dev-tools \
            libappindicator3-dev

      - name: Install deps (macOS)
        if: runner.os == 'macOS'
        run: |
          brew update
          brew install cmake ninja curl openssl@3 libevent qt@6 gtk4 gtkmm4

      # -------- Build --------
      - name: Configure
        run: |
          cmake -S . -B build \
            -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            -DENABLE_CLI=ON \
            -DENABLE_DAEMON=ON \
            -DENABLE_UTILS=ON \                    # 编译 transmission-remote 等工具
            -DENABLE_GTK=${{ runner.os == 'Linux' && 'ON' || 'OFF' }} \
            -DENABLE_QT=ON \
            -DENABLE_MAC=${{ runner.os == 'macOS' && 'ON' || 'OFF' }}

      - name: Build
        run: |
          if [[ "$RUNNER_OS" == "macOS" ]]; then
            ninja -C build transmission-mac transmission-cli transmission-daemon transmission-remote transmission-create transmission-edit transmission-show
          else
            cmake --build build --parallel
          fi

      # -------- Package --------
      - name: Package
        shell: bash
        run: |
          mkdir -p dist
          if [[ "$RUNNER_OS" == "Linux" ]]; then
            cp build/daemon/transmission-daemon dist/ 2>/dev/null || true
            cp build/cli/transmission-cli dist/ 2>/dev/null || true
            cp build/utils/transmission-remote dist/ 2>/dev/null || true
            cp build/utils/transmission-create dist/ 2>/dev/null || true
            cp build/utils/transmission-edit dist/ 2>/dev/null || true
            cp build/utils/transmission-show dist/ 2>/dev/null || true
            cp -r build/gtk dist/ 2>/dev/null || true
          elif [[ "$RUNNER_OS" == "macOS" ]]; then
            cp build/transmission-mac dist/Transmission.app 2>/dev/null || true
            cp build/cli/transmission-cli dist/ 2>/dev/null || true
            cp build/daemon/transmission-daemon dist/ 2>/dev/null || true
            cp build/utils/transmission-* dist/ 2>/dev/null || true
          fi
          
          tar -czf transmission-${{ steps.version.outputs.version }}-${{ runner.os }}.tar.gz dist

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: transmission-${{ runner.os }}
          path: transmission-${{ steps.version.outputs.version }}-${{ runner.os }}.tar.gz

  release:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.event.inputs.version }}
          name: "Transmission ${{ github.event.inputs.version }}"
          body: |
            ## Transmission ${{ github.event.inputs.version }}
            
            **Included Tools:**
            - transmission-cli
            - transmission-daemon
            - transmission-remote
            - transmission-create
            - transmission-edit
            - transmission-show
          draft: false
          prerelease: false
          files: |
            **/*.tar.gz
